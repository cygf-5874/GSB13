[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "repropack"
version = "0.2.4"
description = "Deterministic directory archiver (stdlib only)"
requires-python = ">=3.9"
dependencies = []

[tool.setuptools]
packages = ["repropack"]
